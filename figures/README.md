# Presentation figures

`R/palette.R` is the shared presentation palette, derived from the public
dashboard's colors. Create and export figures in scripts such as
`R/swp_leadership_mtg_figs.R`.

Write generated PNG, SVG, and PDF files to `output/`. The source code is tracked;
the generated output is not.
